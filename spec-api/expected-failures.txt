# Newman request names ("Folder / Request") that are allowed to fail because the endpoint is stubbed.
# Empty: every request in the collection passes. Add an entry only when an endpoint is deliberately
# stubbed; remove it when the endpoint is implemented.
